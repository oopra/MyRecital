{
  "name": "myrecital",
  "version": "1.0.0",
  "description": "Turn a text story into an animated short, edit every scene, and record it as a video.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "lint": "eslint js/",
    "check": "for f in js/*.js; do node --check \"$f\" || exit 1; done",
    "serve": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "playwright": "^1.56.0"
  }
}
